"use client";

/**
 * Intentionally unused: the site forces dark theme via `Providers` (`forcedTheme="dark"`).
 * Kept for the documented component layout.
 */
export function ThemeToggle() {
  return null;
}
